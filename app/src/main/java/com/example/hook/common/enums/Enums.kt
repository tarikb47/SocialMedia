package com.example.hook.common.enums

enum class MessageType {
    TEXT,
    IMAGE,
    VIDEO,
    FILE
}
enum class MessageStatus {
    SENT,
    DELIVERED,
    READ
}
enum class FieldType {
    USERNAME, EMAIL, PHONE_NUMBER
}